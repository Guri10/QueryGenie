# Git
.git
.gitignore
.github

# Python
__pycache__
*.pyc
*.pyo
*.pyd
.Python
venv/
env/
ENV/
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Data and models
# Note: For free tier (no persistent disks), we include FAISS index in image
# Exclude paper JSON files (too large), but allow index files to be copied
data/arxiv_papers_*.json
data/*.pkl
# FAISS index files are included in Dockerfile for free tier deployment
# data/*.faiss and data/*_chunks.json are NOT ignored (needed in image)
models/  # Models will be downloaded on first run if USE_LLM=true

# Logs
*.log
logs/

# Environment
.env
.env.local

# Documentation
*.md
!README.md

# Docker
Dockerfile*
docker-compose*.yml
.dockerignore

# Test files
test_*.py
*_test.py
test_queries.json

# Scripts that aren't needed in container
scripts/
download_more_papers.py
optimize_querygenie.sh

# Frontend (handled separately)
frontend/
web/

